# .bashrc
#
# To define user specific aliases and functions. 
# .bashrc is executed every time a terminal or a sub-shell is launched. 
# 

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# Source group environment setup
if [ -f /nfs/slac/g/lcls/build/epics/setup/epicsReset.bash ]; then
    . /nfs/slac/g/lcls/build/epics/setup/epicsReset.bash
fi

# Your customization goes below:
alias rm='rm -i'
alias purge='/bin/rm '*~''

